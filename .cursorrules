# Alex Miaosha Mobile - AI 指令与开发规范 (.cursorrules)

本文件定义了在该项目中与 AI (Antigravity/Cursor) 协作时必须遵守的所有底层规范与最佳实践。

---

## 1. 全局 UI 架构与视觉标准
项目中所有的 UI 改造与新组件开发需遵循以下基准：

- **组件视觉形态 (Visual Hierarchy)**：
  - 推崇“去线留白”。顶部搜索与下拉导航尽量去除底部边界阴影。
  - 核心内容实施**卡片化布局**，圆角基准统一提高为 `16px`。
  - 列表容器需保持适当侧边距（推荐 `padding: 0 16px`）。
- **交互体验 (Interaction & Haptics)**：
  - **触觉反馈 (Haptic)**：在关键业务按钮（提交、删除、列表卡片点击跳转）中，需通过 `navigator.vibrate?.(50)` 注入触力动效震动。
  - **原生手势兼容**：卡片通过 `active` 状态缩放 (`&:active { transform: scale(0.98); }`) 拟合原生级按压动效。
- **加载与感知 (Performance)**：
  - 加载占位符首推骨架屏（Skeleton Screen，如 `<van-skeleton>`）而非 Loading 文本。
  - 空状态应统一使用插画友好提示补充。

---

## 2. 公共基础组件与 Hooks 使用规范

### 📌 核心公共组件 (src/views/components/)
- **`CommonPullRefresh.vue`**: 对 `van-pull-refresh` 的轻量封装，仅负责手势拦截。
- **`CommonList.vue`**: 负责列表状态（加载中、已结束、空状态）的统一显示，集成骨架屏。

### 📌 核心 Hooks (src/composables/)
- **`usePagination.ts`**: 标准化分页管理。禁止在业务组件内独立定义 `ref({ current: 1, ... })`，统一使用 `usePagination`。

### 📌 自动导入说明 (Auto-Imports)
- 项目使用 `unplugin-auto-import` 和 `unplugin-vue-components`。
- **Vue APIs**: `ref`, `computed`, `watch`, `onMounted` 等无需显式 import。
- **UI Components**: Vant 组件 (`van-xxx`) 及 `src/views/components/` 下的组件可直接在 template 中使用，无需显式 import。

---

## 3. 开发规范与代码约束

### 📌 交付质量要求
- **ESLint 校验**: 任何代码修改后，必须运行 `npm run lint` 确保无报错。
- **基础测试**: 修改关键逻辑后需运行 `npm run test` 或在本地环境下进行功能验证，严禁提交无法运行的代码。

### 📌 现代 API 标准
- **严禁使用 `url.parse()`**: 必须使用 `new URL()` API 替换。
- **类型安全**: 必须使用 TypeScript，优先定义 Interface 而非 type。
- **API 调用**: 优先使用 `postData`, `getData`, `putData`, `deleteData` 等封装好的核心抽象。

### 📌 Vue SFC 结构顺序
1. `<template>`
2. `<script setup lang="ts">`
3. `<style lang="less" scoped>`

### 📌 Script 内部声明顺序
1. **Imports**：依赖导入。
2. **Constants / Types / API**：静态定义。
3. **useHooks**：路由、Store、Composables 实例化。
4. **Variables**：`ref`, `reactive`, `computed`, `defineProps` 等。
5. **Methods**：业务逻辑函数（`init`/`fetchData` 置于该区底部）。
6. **Lifecycle**：`onMounted` 等生命周期。
7. **Watchers**：`watch` 监听。
8. **Emits**：`defineEmits` 必须作为 Script 区块的最后一行。

---

## 4. 知识图谱维护 (Graphify)
项目使用 `graphify` 进行架构分析与关系追踪。
- **修改后同步**: 凡修改 `src` 目录下的代码文件，完成后必须运行 `npm run graphify:update` 以保持图谱同步。
- **查询参考**: 在推断跨模块依赖或进行重构分析时，优先查询 `src/graphify-out/GRAPH_REPORT.md`。

---

## 5. 「开发/架构层面」双向同步协议
> [!IMPORTANT]
> **凡涉及全局交互体系、基础设施、或 `src/views/components` 的结构性更改，Antigravity 必须自觉检查并更新本 `.cursorrules` 信息（或同步更新 `DEVELOPMENT.md`）。**

---

## 6. UI/UX 设计智能 (UI/UX Pro Max)
在进行任何 UI/UX 相关工作时，必须启用 `ui-ux-pro-max` 技能：
- **核心流程**: 使用 `python .gemini/skills/ui-ux-pro-max/scripts/search.py "关键词" --design-system` 获取方案。
- **严禁使用 Emoji**: 必须使用 SVG。
